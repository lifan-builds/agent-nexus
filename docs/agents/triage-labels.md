# Agent Triage Labels

Triage labels are intentionally unconfigured because Matt Pocock's issue workflow skills are not deployed for this repo right now.

If `triage` is added back later, define the label mapping here before using it:

| Role | Label |
| ---- | ----- |
| `needs-triage` | TBD |
| `needs-info` | TBD |
| `ready-for-agent` | TBD |
| `ready-for-human` | TBD |
| `wontfix` | TBD |
