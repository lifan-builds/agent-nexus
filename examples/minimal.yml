name: minimal-agent-nexus
version: 1.0.0

targets:
  - claude
  - cursor

packages:
  - repo: lifan-builds/context-harness
    ref: main
    hooks: false

mcps:
  - name: context7
    command: npx
    args: ["-y", "@upstash/context7-mcp@latest"]

optional_mcps: []
